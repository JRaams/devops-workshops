name: API

on:
  push:
    paths:
      - "api/**"
  pull_request:
    paths:
      - "api/**"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x, 12.x]
    steps:
      # 1. Checks-out the repository under $GITHUB_WORKSPACE
      - name: 1. Checkout repo
        uses: actions/checkout@v2

      # 2. Setup node.js environment
      - name: 2. Setup node
        uses: actions/setup-node@v2.1.4

      # 3. Change directory to API folder
      - name: 3. Cd to /api
        run: cd api

      # 4. Clean node_modules
      - name: 4. Npm clean install
        run: npm ci

      # 5. Build
      - name: 5. Npm build
        run: npm run build --if-present

      # 6. Run eslint
      - name: 6. Run eslint
        run: npm run lint

      # 7. Run tests
      - name: 7. Run tests
        run: npm run test
